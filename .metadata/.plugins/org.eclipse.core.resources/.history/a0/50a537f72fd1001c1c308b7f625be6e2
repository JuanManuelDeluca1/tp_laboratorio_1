/*
 ============================================================================
 Name        : Parcial.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>

#include "Tipo.h"
#include "Aerolinea.h"
#include "Avion.h"

#define TAMA 300
#define TAMT 4
#define TAMAE 5

int main(void)
{
	setbuf(stdout,NULL);
	char salir;
	int flagCarga = 0;
	int indice;
	int proximoIdAvion = 10000;
	eAvion aviones[300];
	eTipo tipo[TAMT] =
	{
	   {5000, "Jet"},
	   {5001, "Helice"},
	   {5002, "Planeador"},
	   {5003, "Carga"}
	};

	eAerolinea aerolinea[TAMAE] =
	{
	   {1000, "Lan"},
	   {1001, "Iberia"},
	   {1002, "Norwegian"},
	   {1003, "American"},
	   {1004, "Austral"}
	};
	do
		{
			switch(menu())
			{
				case 1:
					if(flagCarga == 0)
					{
						inicializarAvion(aviones, TAMA);
						buscarAvionLibre(aviones, TAMA, &indice);
						flagCarga = 1;
					}
					if(flagCarga == 1)
					{
						altaAvion(aviones, TAMA, tipo, TAMT, aerolinea, TAMAE, &proximoIdAvion);
					}
					else
					{
						printf("Ubo un problema al cargar\n");
					}
				break;
				case 2:
					if(flagCarga == 1)
					{
						modificarAvion(aviones, TAMA, tipo, TAMT, aerolinea, TAMAE, &proximoIdAvion);
					}
					else
					{
						printf("debe cargar un pasajero primero\n\n");
					}
				break;
				case 3:
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
					{
						printf("debe cargar un pasajero primero\n\n");
					}
				break;
				case 4:
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
					{
						printf("debe cargar un pasajero primero\n\n");
					}
				break;
				case 5:
					if(flagCarga == 0)
					{
						printf("cagrgo");
					}
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
					{
						printf("problema al agregar pasajero\n");
					}
				break;
				case 6:
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
					{
						printf("debe cargar un pasajero primero\n\n");
					}

				break;
				case 7:
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
					{
						printf("debe cargar un pasajero primero\n\n");
					}

				break;
				case 8:
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
			        {
						printf("debe cargar un pasajero primero\n\n");
					}

				break;
				case 9:
					if(flagCarga == 1)
					{
						printf("cagrgo");
					}
					else
					{
						printf("debe cargar un pasajero primero\n\n");
					}

				break;
				case 10:
					confirmarSalida(&salir);
				break;
				default:
					printf("opcion Invalida!\n\n");
			}
			system("pause");

		}while(salir != 's');
	return 0;
}

