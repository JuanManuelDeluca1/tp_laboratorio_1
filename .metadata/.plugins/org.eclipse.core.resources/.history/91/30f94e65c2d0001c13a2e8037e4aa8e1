/*
 * marca.c
 *
 *  Created on: 9 may. 2022
 *      Author: juanm
 */

#include <stdio.h>
#include <stdlib.h>

int mostrar(eColores vec[], int tam)
{
	int todoOk = 0;

	    if(vec != NULL && tam > 0){
	     // system("cls");
	        printf("   *** Lista de Sectores  ***\n");
	        printf("  Id    Descripcion\n");
	        printf("-----------------------\n");
	        for(int i=0; i < tam; i++){
	            printf("   %4d    %10s\n", vec[i].id, vec[i].descripcion);
	        }
	        printf("\n\n");
	        todoOk = 1;
	    }
	 return todoOk;
}
