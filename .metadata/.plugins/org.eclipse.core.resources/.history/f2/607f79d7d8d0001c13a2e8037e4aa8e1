/*
 * marca.c
 *
 *  Created on: 9 may. 2022
 *      Author: juanm
 */

#include <stdio.h>
#include <stdlib.h>

#include "marca.h"

void mostrarMarcas(eMarcas e)
{

    printf("%d      %s\n", e.id, e.descripcion);

}


int listarMarcas(eAuto vec[], int tam)
{
    int todoOk = 0;
    if(vec != NULL && tam > 0)
    {
        //system("cls");
        printf("          *** Listado de Pasajeros ***\n\n");
        printf("id    idMarca    idColor    tipoCaja\n");
        printf("-----------------------------------------------------\n");
        for(int i=0; i < tam; i++)
        {
        	if(!vec[i].isEmpty)
        	{
        		mostrarColores(vec[i]);
        	}
        }
        printf("\n\n");

        todoOk = 1;
    }
    return todoOk;
}

